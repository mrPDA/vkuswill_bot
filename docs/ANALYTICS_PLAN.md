# План аналитики: бот-помощник ВкусВилл

> Подготовлено: 2026-02-13
> Принцип: минимум событий → максимум решений
> Инфраструктура: PostgreSQL `user_events` (уже есть)

---

## Принципы

1. **Каждое событие должно отвечать на конкретный бизнес-вопрос.**
   Если не можешь назвать решение, которое зависит от этих данных — не логируй.

2. **Бизнес-события и технические метрики — в разных местах.**
   `user_events` — только бизнес-события (десятки в день на юзера).
   Технические метрики (токены, latency) — в логах / Prometheus / отдельная таблица.

3. **Никогда не хранить текст сообщений в событиях.**
   Ни `user_message`, ни `bot_response`. Это персональные данные,
   они уже есть в истории диалога (Redis/memory). В событиях — только агрегаты.

4. **Deep links для атрибуции источника.**
   Хабр: `t.me/vkusvill_bot?start=habr`
   Реферал: `t.me/vkusvill_bot?start=ref_<user_id>`
   Органика: `t.me/vkusvill_bot?start=` (пустой или без параметра)

---

## Уровень 1: Три критических события (P0)

Реализовать **до публикации на Хабре**. Без них теряем
невосполнимые данные о первых пользователях.

### 1.1. `bot_start` — пользователь запустил бота

```
Где:       handlers.py → cmd_start()
Когда:     каждый вызов /start
Решения:   откуда приходят пользователи, размер когорт
```

```python
metadata: {
    "source": "habr" | "referral" | "organic",  # из deep link
    "referrer_id": 12345 | null,                  # если start=ref_12345
    "is_new_user": true | false                    # первый /start или повторный
}
```

**Как определить source:**
```python
# В cmd_start():
args = message.text.split(maxsplit=1)
start_param = args[1] if len(args) > 1 else ""

if start_param.startswith("ref_"):
    source = "referral"
    referrer_id = int(start_param[4:])
elif start_param in ("habr", "vc", "telegram"):
    source = start_param
else:
    source = "organic"
```

---

### 1.2. `cart_created` — корзина успешно создана

```
Где:       tool_executor.py → postprocess_result() после _save_cart_snapshot
Когда:     успешный vkusvill_cart_link_create
Решения:   activation rate, средний чек, GMV, unit economics
```

```python
metadata: {
    "cart_number": 3,               # какая по счёту у пользователя
    "items_count": 7,               # товаров в корзине
    "total_sum": 1450,              # оценочная сумма (₽), из calc_total
    "scenario": "recipe" | "search" | "budget" | "mixed",
    "messages_in_session": 5,       # сообщений до корзины в этой сессии
    "affiliate_tag": "vkbot_abc"    # партнёрская метка в ссылке (если есть)
}
```

**Определение `scenario`:**
Не по входному тексту (невозможно заранее), а по факту — какие tools
были вызваны в текущем FC-цикле:
- Был `recipe_ingredients` → `"recipe"`
- Был `nutrition_calculate` → `"kbju"` (→ но если была и корзина, то `"mixed"`)
- Только `vkusvill_products_search` → `"search"`
- Были оба → `"mixed"`

---

### 1.3. `cart_limit_reached` — пользователь упёрся в лимит

```
Где:       tool_executor.py → execute(), перед вызовом cart_link_create
Когда:     carts_created >= cart_limit
Решения:   какой % доходит до лимита, когда показывать survey
```

```python
metadata: {
    "carts_used": 5,
    "cart_limit": 5,
    "days_since_registration": 12,
    "tier": 1                       # 1 = free, 2 = после survey
}
```

---

## Уровень 2: Ещё четыре события (P1)

Реализовать **в первую неделю после запуска**, когда появятся
реальные пользователи и станет понятно, что мониторить.

### 2.1. `session_start` — начало сессии

```
Где:       middlewares.py → UserMiddleware
Когда:     сообщение, если last_message_at > 30 мин назад
Решения:   DAU, retention, частота использования
```

```python
metadata: {
    "day_number": 5,                # дней с регистрации
    "sessions_today": 2,            # какая сессия за сегодня
    "days_since_last_session": 3    # дней с прошлой сессии
}
```

**Важно:** НЕ делать лишний SELECT. Поле `last_message_at` уже есть
в `db_user`, который загружается в UserMiddleware. Сравниваем:

```python
last = db_user.get("last_message_at")
if last is None or (datetime.now(UTC) - last).total_seconds() > 1800:
    await self._user_store.log_event(user_id, "session_start", {...})
```

Нулевой лишний запрос — данные уже в памяти.

---

### 2.2. `survey_completed` — пользователь заполнил опрос

```
Где:       handlers.py → survey callback / handle_text (текстовый отзыв)
Когда:     последний шаг survey (кнопка «Всё отлично» или текстовый ввод)
Решения:   PMF (Sean Ellis test), приоритеты фич, качественная обратная связь
```

```python
metadata: {
    "pmf": "very" | "somewhat" | "not",        # Sean Ellis: как расстроитесь?
    "useful_feature": "search" | "recipe" | "cart" | "other",
    "feedback": "Хочу больше рецептов" | null,  # текстовый отзыв (до 500 символов)
    "carts_before_survey": 5,
    "days_since_registration": 8
}
```

---

### 2.3. `product_search` — поиск товара

```
Где:       tool_executor.py → postprocess_result() для vkusvill_products_search
Когда:     каждый вызов поиска
Решения:   популярные запросы, % пустых результатов, воронка search→cart
```

```python
metadata: {
    "query": "молоко",              # поисковый запрос (не сообщение юзера!)
    "results_count": 5,
    "had_results": true
}
```

**Заметка:** `query` — это search_query из аргументов tool call,
а не текст пользователя. Это не персональные данные, а параметр поиска.

---

### 2.4. `bot_error` — ошибка при обработке

```
Где:       handlers.py → handle_text() except block
Когда:     любое необработанное исключение
Решения:   стабильность бота, частые ошибки
```

```python
metadata: {
    "error_type": "RateLimitError" | "TimeoutError" | "MCPError" | "Unknown",
    "error_message": "429 Too Many Requests",   # без стектрейса
    "had_history": true                          # был ли контекст диалога
}
```

---

## Уровень 3: Технические метрики (P2)

НЕ в `user_events`. Отдельно — в логах или в лёгкую таблицу.

### 3.1. Токены и стоимость — в логах (structlog)

```python
# В gigachat_service._extract_usage():
logger.info(
    "llm_usage",
    user_id=user_id,
    call_number=call_number,
    model=model_name,
    prompt_tokens=usage.prompt_tokens,
    completion_tokens=usage.completion_tokens,
    precached_tokens=getattr(usage, 'precached_prompt_tokens', 0),
    billable_tokens=billable,
    response_time_ms=elapsed_ms,
    function_name=function_name or "",
)
```

Агрегация: через `grep` / `jq` / Loki / Prometheus — позже.
Не нужно писать в PostgreSQL каждый LLM-вызов.

### 3.2. Простая таблица агрегатов (для /admin_analytics)

```sql
-- Миграция 003: Ежедневные агрегаты (заполняется cron/периодической задачей)
CREATE TABLE IF NOT EXISTS daily_stats (
    date            DATE        PRIMARY KEY,
    dau             INTEGER     NOT NULL DEFAULT 0,
    new_users       INTEGER     NOT NULL DEFAULT 0,
    sessions        INTEGER     NOT NULL DEFAULT 0,
    carts_created   INTEGER     NOT NULL DEFAULT 0,
    total_gmv       NUMERIC     NOT NULL DEFAULT 0,
    searches        INTEGER     NOT NULL DEFAULT 0,
    errors          INTEGER     NOT NULL DEFAULT 0,
    tokens_used     BIGINT      NOT NULL DEFAULT 0,
    tokens_cost_rub NUMERIC     NOT NULL DEFAULT 0
);
```

---

## Ограничения (честно)

| Что хотим знать | Можем ли? | Почему |
|---|:---:|---|
| Кликнул ли пользователь по ссылке на корзину | **Нет** | Ссылка ведёт на VkusVill, мы не контролируем |
| Оформил ли реальный заказ | **Нет** | Нет API от VkusVill для проверки |
| Какой реальный чек (не наша оценка) | **Нет** | Пользователь может изменить корзину на сайте |
| Стал ли пользователь клиентом ВВ | **Косвенно** | Через affiliate-отчёт partners.vkusvill.ru |
| Тип запроса до обработки LLM | **Нет** | Определяется только постфактум по tool calls |

**Для питча ВкусВиллу:** быть честным — наши цифры GMV
это «сумма корзин, сгенерированных ботом», а не подтверждённые заказы.
Формулировка: «Бот сгенерировал X корзин на сумму Y₽.
По нашей оценке, ~30% конвертируются в заказы = ~Z₽ GMV.»

---

## Data Lifecycle

| Данные | TTL | Действие |
|---|---|---|
| `user_events` (P0/P1) | 12 месяцев | Старше 12 мес → удалить или archive |
| `daily_stats` (агрегаты) | Бессрочно | Маленькая таблица, не растёт |
| `users` | Бессрочно | Основная таблица |
| Structured logs (LLM) | 30 дней | Ротация логов |

Защита от переполнения:
```sql
-- Cron: ежемесячно удалять события старше 12 месяцев
DELETE FROM user_events WHERE created_at < NOW() - INTERVAL '12 months';
```

При 1,000 DAU × 10 событий/день × 365 дней = ~3.6M записей/год.
С JSONB ~500 байт каждая = ~1.8 GB. Приемлемо для PostgreSQL.

---

## Команда /admin_analytics

Вместо сырых SQL-запросов — команда в боте:

```
/admin_analytics          — за сегодня
/admin_analytics week     — за неделю
/admin_analytics month    — за месяц
```

Вывод:
```
📊 Аналитика за 7 дней (06.02 – 13.02)

Пользователи:
  Новых:           47
  DAU (средний):   83
  Retention D1:    34%
  Retention D7:    12%

Корзины:
  Создано:         312
  Средний чек:     1,380₽
  GMV (оценка):    430,560₽
  Ср. корзин/юзер: 3.8

Воронка:
  /start → сообщение:    89%
  Сообщение → поиск:     72%
  Поиск → корзина:       58%
  Корзина → лимит:       23%
  Лимит → survey:        61%

Расходы:
  Токенов:         4.2M
  Стоимость API:   2,730₽
  На пользователя: 32₽

Ошибки:           7 (2.2% запросов)
```

---

## План реализации

| # | Задача | Файлы | Статус |
|---|---|---|:---:|
| 1 | Deep links в /start | `handlers.py` | DONE |
| 2 | Событие `bot_start` | `handlers.py` | DONE |
| 3 | Событие `cart_created` | `tool_executor.py` | DONE |
| 4 | Событие `product_search` | `tool_executor.py` | DONE |
| 5 | Событие `session_start` | `middlewares.py` | DONE |
| 6 | Событие `bot_error` | `handlers.py` | DONE |
| 7 | Structured logging для LLM | `gigachat_service.py` | DONE |
| 8 | Migration 003 (daily_stats) | `migrations/003_daily_stats.sql` | DONE |
| 9 | StatsAggregator (фоновая агрегация) | `services/stats_aggregator.py` | DONE |
| 10 | /admin_analytics | `handlers.py` | DONE |
| 11 | /admin_funnel | `handlers.py` | DONE |
| 12 | Событие `cart_limit_reached` | `tool_executor.py` | DONE |
| 13 | Событие `survey_completed` | `handlers.py` | DONE |
| 14 | Дашборд Metabase | `docker-compose.yml` | DONE |

---

## Фаза 2: Визуальный дашборд (Metabase)

### Почему Metabase

- Open-source (AGPL), бесплатный self-hosted
- Подключается напрямую к PostgreSQL (те же `daily_stats` и `user_events`)
- Автообновление дашбордов, мобильная версия
- Минимальная настройка: Docker + переменные окружения

### Развёртывание

Добавить сервис в `docker-compose.yml`:

```yaml
metabase:
  image: metabase/metabase:latest
  ports:
    - "3001:3000"    # 3000 занят Langfuse
  environment:
    MB_DB_TYPE: postgres
    MB_DB_DBNAME: metabase
    MB_DB_PORT: 5432
    MB_DB_USER: vkuswill
    MB_DB_PASS: vkuswill_dev
    MB_DB_HOST: postgres
  depends_on:
    postgres:
      condition: service_healthy
  deploy:
    resources:
      limits:
        memory: 1G
```

### Дашборды

**Дашборд 1: Ежедневный обзор** (источник: `daily_stats`)
- DAU, новые пользователи, сессии (линейный график)
- Корзины и GMV (столбчатая диаграмма)
- Средний чек (числовое значение)
- Ошибки (индикатор)

**Дашборд 2: Воронка** (источник: `user_events`)
- Воронка: start → session → search → cart → limit → survey
- Конверсия между этапами
- Фильтр по дням

**Дашборд 3: Источники трафика** (источник: `user_events`)
- Распределение по source (habr, referral, organic)
- Конверсия по источнику
- Когортный анализ

### SQL-запросы для настройки

Метрики за последние 7 дней:
```sql
SELECT date, dau, new_users, carts_created, total_gmv
FROM daily_stats
WHERE date >= CURRENT_DATE - 7
ORDER BY date;
```

Источники трафика:
```sql
SELECT
    metadata->>'source' AS source,
    COUNT(*) AS users,
    COUNT(CASE WHEN (metadata->>'is_new_user')::boolean THEN 1 END) AS new_users
FROM user_events
WHERE event_type = 'bot_start'
  AND created_at >= CURRENT_DATE - 30
GROUP BY metadata->>'source'
ORDER BY users DESC;
```

### Когда переходить на Metabase

- **< 100 MAU**: достаточно `/admin_analytics` + `/admin_funnel`
- **100–500 MAU**: развернуть Metabase, настроить 3 дашборда
- **> 500 MAU**: рассмотреть DataLens (если уже в Yandex Cloud)

---

## Связанные документы

- [FREEMIUM_STRATEGY.md](./FREEMIUM_STRATEGY.md) — лимиты корзин, survey
- [PRICING_STRATEGY.md](./PRICING_STRATEGY.md) — монетизация, affiliate
- [TOKEN_OPTIMIZATION_PLAN.md](./TOKEN_OPTIMIZATION_PLAN.md) — оптимизация расходов
