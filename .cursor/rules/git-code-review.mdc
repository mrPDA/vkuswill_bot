---
description: Правила Code Review
alwaysApply: true
---

# Правила Code Review

## Обязанности автора PR

### Перед созданием PR

- [ ] Код протестирован локально
- [ ] Все тесты проходят: `uv run pytest`
- [ ] Код соответствует стандартам проекта
- [ ] Нет закомментированного кода
- [ ] Удалены debug-выражения (print, breakpoint)
- [ ] Обновлена документация (если нужно)
- [ ] PR имеет понятное описание

### Во время ревью

- Отвечайте на комментарии в течение 24 часов
- Объясняйте свои решения, если они не очевидны
- Будьте открыты к обратной связи
- Вносите исправления быстро

## Обязанности ревьюера

### Что проверять

1. **Функциональность**
   - Код решает задачу?
   - Нет ли багов?
   - Обработаны все edge cases?

2. **Архитектура**
   - Код в правильном месте?
   - Не нарушены принципы SOLID?
   - Нет дублирования?

3. **Читаемость**
   - Понятны ли названия переменных/функций?
   - Есть ли комментарии где нужно?
   - Сложная логика объяснена?

4. **Тесты**
   - Есть тесты для новой функциональности?
   - Покрытие достаточное?
   - Тесты проверяют важные сценарии?

5. **Безопасность**
   - Нет утечек секретов?
   - Валидируются входные данные?
   - Обрабатываются ошибки?

### Как оставлять комментарии

#### ✅ ХОРОШИЕ комментарии

```markdown
# Конструктивный
Предлагаю использовать `async with` для автоматического закрытия соединения:

```python
async with aiohttp.ClientSession() as session:
    response = await session.get(url)
```

Это гарантирует, что ресурсы освободятся даже при ошибке.
```

```markdown
# Вопрос для понимания
Почему здесь используется `time.sleep()` вместо `asyncio.sleep()`?
Это может блокировать event loop.
```

```markdown
# Похвала
Отличное решение с использованием кэша! Это значительно ускорит повторные запросы.
```

#### ❌ ПЛОХИЕ комментарии

```markdown
# Неконструктивный
Это плохо.
```

```markdown
# Неуважительный
Кто вообще так пишет код?
```

```markdown
# Без объяснения
Переделай.
```

## Типы комментариев

Используйте префиксы для ясности:

- **[NIT]**: небольшое предложение, не критично
- **[BLOCKER]**: критичная проблема, требует исправления
- **[QUESTION]**: вопрос для понимания
- **[SUGGESTION]**: предложение по улучшению
- **[PRAISE]**: похвала за хорошее решение

**Примеры:**

```markdown
[NIT]: Можно использовать f-string вместо format()

[BLOCKER]: Здесь возможна утечка памяти при большом количестве запросов

[QUESTION]: Почему выбран именно этот алгоритм?

[SUGGESTION]: Рассмотрите использование dataclass для этой структуры

[PRAISE]: Отличная обработка ошибок!
```

## Процесс ревью

1. **Первый просмотр** (5-10 мин)
   - Общая архитектура
   - Размер изменений адекватен?
   - Есть ли очевидные проблемы?

2. **Детальный просмотр** (20-40 мин)
   - Построчный анализ
   - Логика корректна?
   - Качество кода

3. **Тестирование** (если нужно)
   - Checkout ветки локально
   - Запуск тестов
   - Проверка функциональности

4. **Итоговая оценка**
   - ✅ **Approve**: код готов к merge
   - 💬 **Comment**: есть предложения, но не критично
   - ❌ **Request changes**: требуются исправления

## Время на ревью

- **Простые PR** (<100 строк): до 2 часов
- **Средние PR** (100-400 строк): до 24 часов
- **Большие PR** (>400 строк): до 48 часов

Если PR срочный, пометьте его соответствующим образом.

## Критерии для merge

- ✅ Минимум 1 approve (для критичных изменений - 2)
- ✅ Все комментарии с [BLOCKER] разрешены
- ✅ Все тесты проходят
- ✅ Нет конфликтов с main
- ✅ CI/CD pipeline успешен (если настроен)
