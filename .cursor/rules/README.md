# Правила ведения репозитория

Этот каталог содержит правила и стандарты для работы с репозиторием проекта vkuswill_bot.

## Список правил

### 📝 [git-commits.mdc](./git-commits.mdc)
Правила оформления коммитов в стиле Conventional Commits.

**Основные принципы:**
- Использование префиксов типов: `feat:`, `fix:`, `docs:`, и т.д.
- Указание области изменений: `(bot)`, `(mcp)`, `(gigachat)`
- Краткие и понятные описания

### 🌿 [git-branches.mdc](./git-branches.mdc)
Правила именования веток для структурированной работы.

**Основные принципы:**
- Формат: `<тип>/<описание>` или `<тип>/<issue>-<описание>`
- Типы: `feature/`, `fix/`, `hotfix/`, `refactor/`, `docs/`, `test/`
- Только строчные буквы с дефисами

### 🔀 [git-pull-requests.mdc](./git-pull-requests.mdc)
Правила создания Pull Request с описанием и чеклистом.

**Основные принципы:**
- Структурированное описание изменений
- Объяснение причины изменений
- Инструкции по тестированию
- Чеклист перед созданием PR

### 👀 [git-code-review.mdc](./git-code-review.mdc)
Правила проведения Code Review для авторов и ревьюеров.

**Основные принципы:**
- Обязанности автора PR
- Обязанности ревьюера
- Типы комментариев: `[NIT]`, `[BLOCKER]`, `[QUESTION]`, `[SUGGESTION]`, `[PRAISE]`
- Критерии для merge

### 🏷️ [git-versioning.mdc](./git-versioning.mdc)
Правила версионирования проекта по Semantic Versioning.

**Основные принципы:**
- Формат: `MAJOR.MINOR.PATCH`
- MAJOR: breaking changes
- MINOR: новая функциональность
- PATCH: исправления багов

## Как использовать

Эти правила автоматически применяются в Cursor при работе с проектом. Cursor AI будет следовать этим стандартам при:

- Создании коммитов
- Создании веток
- Создании Pull Request
- Проведении Code Review
- Управлении версиями

## Быстрый старт

### Создание новой ветки

```bash
git checkout -b feature/add-new-command
```

### Создание коммита

```bash
git add .
git commit -m "feat(bot): добавить команду /status"
```

### Создание релиза

```bash
# Обновите версию в pyproject.toml
git tag -a v1.0.0 -m "Release v1.0.0"
git push origin v1.0.0
```

## Примеры workflow

### Разработка новой функции

```bash
# 1. Создать ветку
git checkout -b feature/webhook-support

# 2. Внести изменения и коммитить
git add src/vkuswill_bot/bot/handlers.py
git commit -m "feat(bot): добавить поддержку вебхуков"

# 3. Отправить в remote
git push origin feature/webhook-support

# 4. Создать PR с описанием по шаблону
```

### Исправление бага

```bash
# 1. Создать ветку от main
git checkout -b fix/42-memory-leak

# 2. Исправить баг
git add src/vkuswill_bot/services/gigachat_service.py
git commit -m "fix(gigachat): исправить утечку памяти в кэше"

# 3. Создать PR
git push origin fix/42-memory-leak
```

## Настройка

Эти правила хранятся в формате `.mdc` и автоматически загружаются Cursor.

Каждый файл содержит:
- **description**: описание правила
- **alwaysApply**: применять ли всегда (true для всех этих правил)
- Детальную документацию с примерами

## Обновление правил

Если вам нужно обновить правила:

1. Отредактируйте соответствующий `.mdc` файл
2. Изменения будут применены автоматически
3. Создайте коммит:
   ```bash
   git commit -m "docs: обновить правила ведения репозитория"
   ```

## Вопросы

Если у вас есть вопросы по правилам или предложения по их улучшению, создайте issue в репозитории.
